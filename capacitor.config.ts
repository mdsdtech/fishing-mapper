import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testing.fishing.mapper',
  appName: 'Fishing Mapper',
  webDir: 'dist'
};

export default config;
